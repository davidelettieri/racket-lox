repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
  - repo: local
    hooks:
      - id: raco-fmt
        name: Racket Format (raco fmt)
        entry: raco fmt -i
        language: system
        types: [file]
        files: \.rkt$
        pass_filenames: true
